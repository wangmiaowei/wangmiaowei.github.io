<div class="publications">
  
  <!-- World Models Section -->
  <h3 id="world-models" style="margin-top: 1.5rem; margin-bottom: 1rem; padding-left: 2rem; border-bottom: 1px solid rgba(0, 0, 0, 0.1); padding-bottom: 0.4rem; font-size: 1.25rem; font-weight: 500;">World Models</h3>
  {% bibliography --group_by none --query @*[key=wang2026motionphysics] %}

  <!-- 4D Generation Section -->
  <h3 id="4d-generation" style="margin-top: 1.5rem; margin-bottom: 1rem; padding-left: 2rem; border-bottom: 1px solid rgba(0, 0, 0, 0.1); padding-bottom: 0.4rem; font-size: 1.25rem; font-weight: 500;">4D Generation</h3>
  {% bibliography --group_by none --query @*[key=wang2026bimotion] %}
  {% bibliography --group_by none --query @*[key=wang2025decoupledgaussian] %}

  <!-- Dynamic Reconstruction Section -->
  <h3 id="dynamic-reconstruction" style="margin-top: 1.5rem; margin-bottom: 1rem; padding-left: 2rem; border-bottom: 1px solid rgba(0, 0, 0, 0.1); padding-bottom: 0.4rem; font-size: 1.25rem; font-weight: 500;">Dynamic Reconstruction</h3>
  {% bibliography --group_by none --query @*[key=wang2025canfields] %}
  {% bibliography --group_by none --query @*[key=wang2024self] %}

</div>
